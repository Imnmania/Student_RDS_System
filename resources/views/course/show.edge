@layout('layouts.app')

@section('body')
<div class="row">
  <div class="col-md-8">
    <h1 class="h3 mb-1 text-gray-800">Details of {{course.course_name}}</h1>

    <div class="card shadow my-4 mx-auto w-40">
      <div class="card-body">
        <ul class="list-group list-group-flush">
          <li class="list-group-item d-flex justify-content-between">
            <b>Course Id</b>
            <span>{{course.course_id}}</span>
          </li>
          <li class="list-group-item d-flex justify-content-between">
            <b>Course Name</b>
            <span>{{course.course_name}}</span>
          </li>
          <li class="list-group-item d-flex justify-content-between">
            <b>Department Id</b>
            <span>{{course.dept_id}}</span>
          </li>
          <li class="list-group-item d-flex justify-content-between">
            <b>Credit Hour</b>
            <span>{{course.credit_hr}}</span>
          </li>
        </ul>
      </div>
    </div>
  </div>
</div>

@endsection
